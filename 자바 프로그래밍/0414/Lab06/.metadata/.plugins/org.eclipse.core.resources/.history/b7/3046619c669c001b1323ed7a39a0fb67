/* 2020114658 이윤서 */
/* 본인은 이 소스파일 다른 사람의 소스를 복사하지 않고 직접 작성하였습니다. */

package q;

import p.B; //default 는 같은 패키지 안에서만 접근 가능

class A {

	void f() {
		// 마찬가지로 접근 불가능
		B b = new B();
		b.n=3;
		b.g();
	}
	
}
